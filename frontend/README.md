# Front-end

Pasta reservada para o front-end do TaskList.

API disponível em `http://localhost:3000`:
- POST /tasks — cria tarefa (body: { "title": "..." })
- GET /tasks — lista tarefas
- PATCH /tasks/:id/complete — marca tarefa como concluída